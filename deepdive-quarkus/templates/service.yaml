apiVersion: v1
kind: Service
metadata:
  name: {{ include "quarkus-deepdive.fullname" . }}
  labels:
    {{- include "quarkus-deepdive.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quarkus-deepdive.selectorLabels" . | nindent 4 }}
